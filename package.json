{
  "name": "kommopp-virtruvia",
  "private": true,
  "version": "0.1.0",
  "description": "Kommo++ VirtruvIA — CRM multi-tenant messenger-first. Monorepo.",
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @kommopp/web",
    "build:web": "npm run build --workspace @kommopp/web",
    "dev:worker": "npm run dev --workspace @kommopp/worker",
    "db:diff": "supabase db diff -f",
    "db:push": "supabase db push",
    "db:test": "supabase db execute --file supabase/tests/rls_isolation_test.sql",
    "test:worker": "npm test --workspace @kommopp/worker",
    "db:test:etapa2": "supabase db execute --file supabase/tests/etapa2_audit_billing_test.sql",
    "db:test:etapa3": "supabase db execute --file supabase/tests/etapa3_crm_test.sql",
    "db:test:etapa4": "supabase db execute --file supabase/tests/etapa4_produtividade_comercial_test.sql"
  },
  "engines": {
    "node": ">=20"
  }
}
